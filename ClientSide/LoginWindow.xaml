<Window x:Class="ChatClient.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="150" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Content="Enter Username:" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <TextBox x:Name="UsernameTextBox" Grid.Row="1" Margin="5" HorizontalAlignment="Center" Width="150"/>
        <Button Content="Connect" Grid.Row="2" Margin="5" HorizontalAlignment="Center" Width="100" Click="ConnectButton_Click"/>
    </Grid>
</Window>
